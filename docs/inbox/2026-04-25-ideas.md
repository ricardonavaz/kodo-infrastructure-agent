# Inbox de Ideas — 2026-04-25

**Día:** Sábado 25 de abril de 2026
**Sesión:** Cierre de Sprint B0, fix arq-16 (playbook screen rendering)
**Contexto general:** Después del fix arq-16 (migración de PlaybookManager a streaming API), el output del playbook se ve correctamente y revela un bug del backend que estaba latente.

---

### IDEA #3 — SQLite error "no such column: 'now'" al guardar playbook runs

**Capturada:** 2026-04-25 ~04:26

**Contexto:** Verificacion visual de arq-16. El streaming de eventos funciona correctamente, pero al final de cada playbook ejecutado aparece un error de SQLite. El error estaba latente (siempre fallaba el guardado del run en BD), pero no se veia porque arq-16 ocultaba todo el output.

**Texto original (de Ricardo, basado en screenshots):**

> "[error] no such column: 'now' - should this be a string literal in single-quotes?"

**Categorización automática:**

- Dominio: código + arquitectura (backend BD)
- Tipo: bug
- Urgencia: alta (afecta persistencia de runs en BD)
- Bloquea trabajo actual: no (arq-16 cerrado tecnicamente)
- Conexiones: arq-16, server/routes/playbooks.js, sistema de runs

**Mi interpretación inicial:**

En el flow de guardar un playbook run en SQLite (probablemente INSERT INTO playbook_runs ... VALUES (..., now, ...)), el codigo usa `now` sin comillas, que SQLite interpreta como nombre de columna en lugar de:
- string literal: 'now'
- funcion: datetime('now') o CURRENT_TIMESTAMP

Sin este fix, los runs probablemente NO se estan guardando en BD (o se guardan con timestamp incorrecto). Eso explicaria si la pestaña "Runs" muestra historico incompleto.

**Fix sugerido:**

1. Localizar el INSERT/UPDATE problematico:
   grep -rn '"now"\|`now`\|, now,' server/
   o
   grep -rn 'INSERT.*playbook_runs\|UPDATE.*playbook_runs' server/

2. Reemplazar con datetime('now') o CURRENT_TIMESTAMP segun mejor practica del schema actual.

3. Verificar que otros INSERTs similares en el codigo no tengan el mismo error.

**Prioridad:** Alta. Bug funcional que afecta persistencia. Atender en proximo fix.

**Estimado:** 30-60 minutos (depende de cuantos lugares tengan el mismo patron).

**Estado:** sin procesar

**Destino propuesto:** crear arq-19 al backlog en proximo procesamiento.

### IDEA #4 — npm audit reporta 1 moderate severity vulnerability post-arq-4.2

**Capturada:** 2026-04-25 ~04:55

**Contexto:** Tras instalar Vitest 3 + RTL 16 + jest-dom 6 + user-event 14 + jsdom 25 en client/, npm reporto "1 moderate severity vulnerability" en el output. No se corrio npm audit fix automaticamente porque modifica deps y excede scope de arq-4.2.

**Texto original (de Claude Code en el reporte):**

> "1 moderate severity vulnerability reportada por npm audit. No corrí npm audit fix automáticamente — eso modifica dependencias y excede el scope."

**Categorización automática:**

- Dominio: código + arquitectura (deps)
- Tipo: observación
- Urgencia: media
- Bloquea trabajo actual: no
- Conexiones: arq-4, npm audit, dependencies del cliente

**Mi interpretación inicial:**

Es probablemente vulnerabilidad transitiva en una de las deps recien instaladas (jsdom o RTL son los candidatos mas probables). Sin saber severity exacta y CVE asociado, no podemos decidir si requiere fix urgente o si esperamos a que upstream actualice.

**Fix sugerido:**

1. Ejecutar npm audit en client/ para ver detalles del CVE
2. Evaluar si es vulnerabilidad de runtime (afecta producto en produccion) o de dev (solo afecta tests/build)
3. Si es de dev: probablemente safe-to-ignore hasta upstream update
4. Si es de runtime: aplicar npm audit fix o pin manual de version segura

**Prioridad:** Media. Ver detalles antes de decidir.

**Estimado:** 30 min para diagnosticar + decidir. Fix puede ser 0 (safe-ignore) hasta 1-2h (refactor de deps).

**Estado:** sin procesar (observación)

**Destino propuesto:** revisar en proximo procesamiento profundo del inbox.

---

### IDEA #5 — SummaryCard renderiza 'null'/'undefined' literal cuando stat.value es null/undefined

**Capturada:** 2026-04-25 ~05:10

**Contexto:** Durante diagnostico de arq-4.3.a, Claude Code detecto que SummaryCard convierte stat.value con String(...). Si stat.value es null o undefined, el resultado es la palabra literal 'null' o 'undefined' en la UI.

**Texto original (de Claude Code):**

> "stat.value=null → String(null) = 'null' literal en UI. Comportamiento sub-optimo pero documentable."

**Categorización automática:**

- Dominio: código + ux
- Tipo: bug + observación
- Urgencia: baja
- Bloquea trabajo actual: no
- Conexiones: SummaryCard.jsx, arq-4.3.a

**Mi interpretación inicial:**

Cuando el backend produce un stat con value: null o value: undefined, la UI muestra "null" o "undefined" como texto. Subóptimo: deberia mostrar string vacio, "—", o "N/A". Pero antes de fix necesitamos saber si esto ocurre en la practica o es caso teorico.

**Fix sugerido:**
1. Investigar si algun stat producido por backend tiene value null/undefined real
2. Si si: cambiar String(stat.value) por algo como (stat.value == null ? '—' : String(stat.value))
3. Si no: documentar contrato "stat.value debe ser primitivo definido" y no fix

**Estado:** sin procesar (observación)

**Destino:** revisar en proximo procesamiento profundo del inbox.

---

### IDEA #6 — Componentes blocks/ asumen block truthy sin defensividad

**Capturada:** 2026-04-25 ~05:10

**Contexto:** Durante diagnostico de arq-4.3.a, Claude Code detecto que los componentes TextBlock, Recommendation, SummaryCard, Finding, QuestionPrompt destructuran block sin defaults. Si block es undefined o null, crashean con TypeError.

**Texto original (de Claude Code):**

> "block undefined → destructuring const {content, format} = undefined → TypeError. No es defensivo."

**Categorización automática:**

- Dominio: código + arquitectura
- Tipo: decisión + observación
- Urgencia: media
- Bloquea trabajo actual: no
- Conexiones: todos los componentes en blocks/, contrato del semantic parser

**Mi interpretación inicial:**

Decision arquitectural pendiente:
- Opcion A: defensividad - cada componente check block && ... y renderiza fallback
- Opcion B: contrato estricto - parent component (semantic parser) garantiza block valido siempre

Hoy operamos con B implicitamente pero sin documentar el contrato. Si el parser falla y produce blocks malformados, la UI crashea entera.

**Fix sugerido:**
1. Decidir formalmente A vs B
2. Si A: agregar defensividad en cada componente
3. Si B: documentar contrato del parser y posiblemente agregar validacion en boundary entre parser y rendering

**Estado:** sin procesar (decisión arquitectural)

**Destino:** procesamiento profundo + decisión formal en futuro docs/decisions/.

---

### IDEA #7 — arq-13 puede estar desactualizado o resuelto

**Capturada:** 2026-04-25 ~05:10

**Contexto:** Durante diagnostico de arq-4.3.a, Claude Code detecto que el codigo de utils/formatMessage.jsx lineas 51 y 56 SI aplica inlineFormat a celdas de tabla, lo que contradice la descripcion del arq-13 en backlog-ideas-producto.md.

**Texto original (de Claude Code):**

> "looking at lines 51 y 56: sí aplican inlineFormat. Así que arq-13 puede estar resuelto o el bug era distinto al que describe el backlog."

**Categorización automática:**

- Dominio: código + arquitectura (backlog accuracy)
- Tipo: pregunta + observación
- Urgencia: baja
- Bloquea trabajo actual: no
- Conexiones: arq-13, formatMessage.jsx

**Mi interpretación inicial:**

Posibilidades:
1. arq-13 ya fue resuelto sin actualizar el backlog
2. El bug era distinto al descrito
3. Hay condiciones edge no cubiertas por las lineas 51, 56 que aun rompen

Necesita verificacion empirica: probar tabla con markdown inline (**bold**, *italic*) y ver renderizado real.

**Fix sugerido:**
1. Render una tabla con celdas conteniendo **bold** y *italic*
2. Confirmar visualmente que se renderiza HTML formateado
3. Si si: cerrar arq-13 como resuelto
4. Si no: mantener arq-13 abierto con descripcion mas precisa

**Estado:** sin procesar (verificación empírica pendiente)

**Destino:** procesamiento profundo del inbox.

---

**Última actualización:** 25 abril 2026, durante diagnostico arq-4.3.a (tests componentes simples)
